const form=document.querySelector('#form'),msg=document.querySelector('#msg'),btn=document.querySelector('#submit');form.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='';if(document.querySelector('#password').value!==document.querySelector('#confirm').value){msg.textContent='As senhas não conferem.';return}btn.disabled=true;btn.textContent='Criando conta...';try{const r=await fetch('/api/lender/register',{method:'POST',body:new FormData(form)});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.detail||'Não foi possível criar a conta.');localStorage.setItem('cred_lender_token',data.token);location.href='/emprestador'}catch(err){msg.textContent=err.message}finally{btn.disabled=false;btn.textContent='Criar minha conta'}});